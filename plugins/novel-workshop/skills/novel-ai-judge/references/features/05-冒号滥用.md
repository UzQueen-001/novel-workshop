# 05 · 冒号滥用

**触发标记**：

- **提示语引出内容** —— 冒号前是「一句话总结」「核心是」「关键在于」「原因如下」「结论」「本质上」「换句话说」这类提示语；
- **空转句引出列表** —— 一行以冒号结尾，紧接着是列表，而这句话去掉后信息不减。

**改法**：提示语不承担信息就删掉它，直接写内容；它在承接上文就保留，只把冒号换成句号或逗号。空转句改写成有内容的判断，或直接删掉让列表接上上文——**列表项不能删**。

**不改**：引出人物原话；小标题里的冒号；列表项内部的冒号；句中引出说明的冒号。
